using System;
using System.Linq;
using System.Collections.Generic;
using System.Web;
using System.Web.Mvc;

using Nemerle;
using Nemerle.NemerleWeb;

namespace MVCTest.Controllers
{
  public class HomeController : Controller
  {
    public Index() : ActionResult 
    {
      View(Utils.MakePageHtml(SamplesViewModel()) : object);
    }
    
    public Tests() : ActionResult
    {
      View(Utils.MakePageHtml(UnitTestViewModel()) : object);
    }
  }
}